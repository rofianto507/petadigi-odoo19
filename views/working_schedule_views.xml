<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- List View untuk Working Schedules -->
        <record id="view_working_schedule_list_smartatt" model="ir.ui.view">
            <field name="name">resource.calendar.list.smartatt</field>
            <field name="model">resource.calendar</field>
            <field name="arch" type="xml">
                <list string="Working Schedules">
                    <field name="name"/>
                    <field name="company_id"/>
                    <field name="hours_per_day" string="Avg. Hours/Day"/>
                </list>
            </field>
        </record>

        <!-- Action untuk Working Schedules -->
        <record id="action_smart_attendance_working_schedules" model="ir.actions.act_window">
            <field name="name">Working Schedules</field>
            <field name="res_model">resource.calendar</field>
            <field name="view_mode">list,form</field>
            <field name="view_ids" eval="[(5, 0, 0), (0, 0, {'view_mode': 'list', 'view_id': ref('view_working_schedule_list_smartatt')})]"/>
        </record>
    </data>
</odoo>